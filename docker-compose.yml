services:
  imperia-api:
    build: .
    env_file:
      - .env
    command: ["uvicorn", "api:app", "--host", "0.0.0.0", "--port", "8000"]
    ports:
      - "8000:8000"
    volumes:
      - imperia_data:/home/appuser/.tradingagents

volumes:
  imperia_data:
